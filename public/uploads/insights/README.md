# Insights upload assets

This directory stores the approved media that ships with published Insights content.

Before committing, delete any temporary or auto-generated Insight images (for example,
those produced during local testing) so they do not end up in a PR. Only keep the
curated assets that are already tracked in Git.
